public class Obstacle {// obstacle class — an entity
    // run — essentially rushes forward toward player until it goes off the screen then deletes itself
    // get x/y — for telling board where to draw things
}
